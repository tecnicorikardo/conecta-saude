{
  "name": "conecta-saude-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm ci --prefix backend --include=dev && npm run build --prefix backend",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "prisma": "^5.14.0"
  },
  "prisma": {
    "schema": "backend/prisma/schema.prisma"
  }
}

